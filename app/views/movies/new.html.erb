<% content_for :title, t('movies.new') %>

<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card">
      <div class="card-header">
        <h3><i class="fas fa-plus"></i> <%= t('movies.new') %></h3>
      </div>
      <div class="card-body">
        <%= render 'form', movie: @movie %>
      </div>
    </div>
  </div>
</div>
